
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.56.10:3306/gmall_pms
    driver-class-name: com.mysql.jdbc.Driver
  #配置nacos注册中心
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  application:
    name: gmall-product
#设置mapper地址
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  #  设置主键自增
  global-config:
    db-config:
      id-type: auto

server:
  port: 10000